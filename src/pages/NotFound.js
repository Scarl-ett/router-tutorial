import React from 'react';

const NotFound = () => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: 64,
        position: 'absolute',
        width: 'absolute',
        height: '100%',
      }}
    >
      404
    </div>
  );
};

export default NotFound;